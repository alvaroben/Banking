<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:InternetBankingApp.Models"
             xmlns:controls="clr-namespace:InternetBankingApp.Controls"
             xmlns:vm="clr-namespace:InternetBankingApp.ViewModels"
             x:Class="InternetBankingApp.Views.CuentasPage"
             x:Name="PageRoot"
             x:DataType="vm:CuentasViewModel"
             Title="Cuentas">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">

            <Button Text="{Binding ToggleButtonText}"
                    HorizontalOptions="Fill"
                    Command="{Binding ToggleFormCommand}" />

            <Border IsVisible="{Binding IsFormVisible}"
                    BackgroundColor="{StaticResource White}"
                    Stroke="{StaticResource Gray200}"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="14" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="12" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}">
                    <Label Text="{Binding FormTitle}" TextColor="{StaticResource MidnightBlue}" FontFamily="OpenSansSemibold" FontSize="17" />

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Tipo de cuenta" TextColor="{StaticResource Gray500}" FontSize="13" />
                        <Picker Title="Selecciona un tipo" ItemsSource="{Binding TiposCuenta}" SelectedItem="{Binding TipoSeleccionado}" />
                        <Label Text="{Binding TipoError}"
                               TextColor="{StaticResource Danger}"
                               FontSize="12"
                               IsVisible="{Binding TipoError, Converter={StaticResource StringNotEmptyConverter}}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label Text="Saldo inicial" TextColor="{StaticResource Gray500}" FontSize="13" />
                        <Entry Text="{Binding Saldo}" Placeholder="0.00" Keyboard="Numeric" />
                        <Label Text="{Binding SaldoError}"
                               TextColor="{StaticResource Danger}"
                               FontSize="12"
                               IsVisible="{Binding SaldoError, Converter={StaticResource StringNotEmptyConverter}}" />
                    </VerticalStackLayout>

                    <Button Text="{Binding GuardarButtonText}"
                            Command="{Binding GuardarCommand}"
                            IsVisible="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

                    <Button Text="Eliminar cuenta"
                            Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding EliminarCommand}"
                            CommandParameter="{Binding CuentaEnEdicion}"
                            IsVisible="{Binding IsEditMode}" />

                    <VerticalStackLayout IsVisible="{Binding IsBusy}" Spacing="8" Margin="0,4,0,0">
                        <controls:LoadingDots x:Name="LoadingIndicator" HeightRequest="20" />
                        <Label Text="Esperando autorización del banco..." TextColor="{StaticResource Gray500}" FontSize="13" HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Label Text="Tus Cuentas" TextColor="{StaticResource MidnightBlue}" FontFamily="OpenSansSemibold" FontSize="17" />

            <CollectionView ItemsSource="{Binding Cuentas}">
                <CollectionView.EmptyView>
                    <Label Text="No hay cuentas aún." TextColor="{StaticResource Gray400}" HorizontalOptions="Center" Margin="0,20" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Cuenta">
                        <Border Stroke="{StaticResource Gray200}" BackgroundColor="{StaticResource White}" Margin="0,0,0,10" Padding="16">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding BindingContext.EditarCommand, Source={x:Reference PageRoot}}" CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto,Auto" RowDefinitions="Auto,Auto">
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding NumeroCuenta}" FontFamily="OpenSansSemibold" FontSize="16" TextColor="{StaticResource MidnightBlue}" />
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding Tipo}" TextColor="{StaticResource EmeraldDark}" FontFamily="OpenSansSemibold" VerticalOptions="Center" />
                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding Saldo, StringFormat='Saldo: {0:C2}'}" TextColor="{StaticResource Gray500}" FontSize="13" />
                                <Label Grid.Row="1" Grid.Column="1" Text="{Binding FechaApertura, StringFormat='{0:dd/MM/yyyy}'}" TextColor="{StaticResource Gray400}" FontSize="13" />
                                <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="2"
                                        Style="{StaticResource DeleteIconButtonStyle}"
                                        VerticalOptions="Center"
                                        Margin="10,0,0,0"
                                        Command="{Binding BindingContext.EliminarCommand, Source={x:Reference PageRoot}}"
                                        CommandParameter="{Binding .}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
